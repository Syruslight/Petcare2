En esta carpeta "BD", se encuentran las diferentes versiones del script de la bd en caso sufra modificaciones para que se actualice constantemente y llevar un historial:

esquema.mwb -->Este documento se puede abrir en MySQL WorkBench para visualizar de mejor manera las relaciones de las tablas (no esta actualizado con el sistema de puntos y el tipo de usuario_usuarios.
petcare_previa.sql --> Este script muestra todas las tablas menos el de sistemas de puntos pero aun sin datos o relaciones en los diagramas.
petcareConDiagrama.sql --> Este script muestra todas las tablas menos el de sistemas de puntos y datos en las tablas, pero ya contiene el diagrama de relaciones.

A desarrollar:
petcareConDatosDiagrama.sql -->Script con todas las tablas menos sistema de puntos con datos para realizar las pruebas (incluye el diagrama de relaciones)
petCareFinal.sql --> Script con todas las tablas y datos correspondientes para verificar todo el funcionamiento del sistema (con el diagrama actualizado teniendo en cuenta el sistema de puntos)

----Encargado de esta parte: Cristhian ----